/**
 * Neste exercício você deve implementar uma função que irá devolver
 * a média de notas de um aluno.
 *
 * Exemplo:
 * mediaDeNotas(10, 8, 9) -> 9
 * mediaDeNotas(10, 10, 10) -> 10
 */

function mediaDeNotas(n1, n2, n3) {
  // Implemente sua solução aqui :)
  return (n1 + n2 + n3) / 3;
}

/* Não modifique a linha abaixo */
module.exports = mediaDeNotas
